# دليل التشغيل والتوزيع العادل — نَجَلَ

## كيف تشغّل كل ملف بالضبط

### 1) `1_backend_main.py` — عضو الـ Backend
```bash
pip install fastapi uvicorn pydantic
# غيّر اسم الملف إلى main.py في مجلده، ثم:
uvicorn main:app --reload
```
افتح `http://127.0.0.1:8000/docs` — بتشوف توثيق تفاعلي لكل الـ endpoints تقدر تجربها مباشرة من المتصفح بدون Frontend.

### 2) `2_frontend_App.jsx` — عضو الـ Frontend
```bash
npx create-react-app najal-frontend
cd najal-frontend
# انسخ محتوى الملف داخل src/App.jsx (استبدل الموجود)
npm start
```
يفتح تلقائياً على `http://localhost:3000` — لازم الـ Backend يكون شغّال بنفس الوقت عشان البيانات تظهر.

### 3) `3_ai_ml_models.py` — عضو AI/ML
```bash
pip install torch torchvision pillow
python models.py
```
بيطبع لك نتائج تجريبية في الـ terminal مباشرة (تشخيص، توصية شجرة، توقع صحة محصول) بدون الحاجة لأي واجهة.

### 4) `4_data_mock_data.py` — مسؤول البيانات
ملف بايثون عادي، يُستورد من الملفين الثاني والثالث:
```python
from mock_data import QASSIM_NEIGHBORHOODS, TREE_TYPES
```
ما يحتاج تشغيل منفصل — هو "مصدر البيانات" اللي البقية يسحبون منه.

---

## التوزيع العادل للكودنق (مو بس "ملف لكل شخص")

المشكلة إن الملفات الأربعة مو متساوية في الصعوبة (AI/ML أصعب من ملف البيانات مثلاً). فبدل "كل واحد ياخذ ملف"، وزّعوا حسب **نقاط جهد** تقريبية، كذا يكون عادل فعلاً:

| المهمة | الملف | نقاط الجهد التقريبية |
|---|---|---|
| بناء endpoints + ربط APIs خارجية | `1_backend_main.py` | 8 |
| بناء الواجهات الثلاث + التصميم | `2_frontend_App.jsx` | 8 |
| نموذج Computer Vision (الأصعب) | `3_ai_ml_models.py` (تشخيص الأمراض) | 9 |
| محرك التوصية + التنبؤ (داخل نفس الملف) | `3_ai_ml_models.py` (الباقي) | 4 |
| تجهيز البيانات + Pitch Deck | `4_data_mock_data.py` + العرض | 7 |

### طريقة التوزيع العادلة الفعلية
بما إن ملف AI/ML وحده = 13 نقطة (الأثقل)، والباقي أخف، اقترح:

- **الشخص 1 (Backend):** كامل `1_backend_main.py` → 8 نقاط
- **الشخص 2 (Frontend):** كامل `2_frontend_App.jsx` → 8 نقاط
- **الشخص 3 (AI - الجزء الصعب فقط):** تشخيص الأمراض (Computer Vision) في `3_ai_ml_models.py` → 9 نقاط
- **الشخص 4 (بيانات + الجزء الأسهل من AI):** `4_data_mock_data.py` (7 نقاط) + محرك التوصية والتنبؤ البسيط من ملف AI (4 نقاط) → 11 نقطة، لكن الجزء الأخير (التوصية) كوده جاهز شبه كامل فوق (Rule-Based بسيط)، فعملياً وقته أقل من رقمه

**الخلاصة:** كل شخص عنده ما يقارب 8-9 "وحدات جهد حقيقي"، والفرق البسيط يتعوّض لأن كود التوصية أعطيتك جاهز تقريباً في `3_ai_ml_models.py`.

---

## نصيحة أخيرة
لا تخلّوا كل شخص "يقفل" على ملفه بالكامل بمعزل عن الباقي. اليوم الأول اجتمعوا 20 دقيقة بس، اتفقوا على:
1. شكل الـ JSON بين Backend و Frontend (موجود جاهز في `1_backend_main.py` كـ Schemas)
2. أسماء الحقول بين AI و Backend (موجودة جاهزة كـ `DiagnosisResult`)

وبعدها كل واحد يشتغل مستقل بدون ما يعطّل التاني.
